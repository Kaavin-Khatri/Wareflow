"""
Concrete repository implementations (SQLAlchemy-backed).

Each module here implements one or more interfaces from ../interfaces/.
These are wired to services via FastAPI's Depends() in core/di.py.
"""
