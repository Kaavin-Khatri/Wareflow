"""
Schemas package — Pydantic models for request/response validation.

These are pure data contracts — no business logic, no database coupling.
One module per domain (e.g., product_schemas.py, order_schemas.py).
"""
